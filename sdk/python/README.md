# Tacto6D SDK

SDK to receive synchronized frames (camera/flow/force) and send control commands to the runtime.  Local-only.

## Install

```bash
pip install -e sdk/python
```

## Test

```bash
python sdk/python/examples/quick_view.py
```
